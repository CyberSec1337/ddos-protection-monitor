/* تنسيق أساسي للجسم */
body {
    background-color: #f8f9fa; /* لون خلفية فاتح */
    font-family: Arial, sans-serif; /* الخط الرئيسي */
    color: #333; /* لون النص */
    margin: 0;
    padding: 0;
}

/* تنسيق العنوان الرئيسي */
header {
    background-color: #007bff; /* لون أزرق فاتح */
    color: white;
    padding: 20px 0;
    text-align: center;
    border-bottom: 3px solid #0056b3;
}

header h1 {
    font-size: 2rem;
    margin: 0;
}

header p {
    font-size: 1rem;
    margin: 5px 0 0;
    color: #dce8f1;
}

/* تنسيق الأقسام */
.container {
    padding: 20px;
}

/* تنسيق الرسومات البيانية */
canvas {
    max-width: 100%;
    height: auto;
    display: block;
    margin: 0 auto;
    background-color: #ffffff;
    border: 1px solid #ccc;
    border-radius: 8px;
}

/* جدول البيانات */
.table-container {
    margin-top: 30px;
}

.table {
    width: 100%;
    border-collapse: collapse;
    margin-top: 20px;
}

.table th,
.table td {
    text-align: left;
    padding: 10px;
    border: 1px solid #ddd;
}

.table th {
    background-color: #007bff; /* لون أزرق */
    color: white;
}

.table tr:nth-child(even) {
    background-color: #f2f2f2;
}

.table tr:hover {
    background-color: #ddd;
}

/* النصوص الملونة حسب الحالة */
.allowed {
    color: #28a745; /* لون أخضر */
    font-weight: bold;
}

.blocked {
    color: #dc3545; /* لون أحمر */
    font-weight: bold;
}

/* مؤشر التحميل */
.loading-spinner {
    display: none; /* يتم عرضها فقط أثناء التحميل */
    text-align: center;
    margin-top: 20px;
}

/* أزرار وتنسيق إضافي */
button {
    background-color: #007bff;
    color: white;
    border: none;
    padding: 10px 15px;
    border-radius: 5px;
    cursor: pointer;
    font-size: 1rem;
}

button:hover {
    background-color: #0056b3;
}

/* للأقسام المختلفة */
.section-title {
    text-align: center;
    margin-bottom: 20px;
    font-size: 1.5rem;
    color: #007bff;
}

/* التصميم الخاص بالرسم البياني */
.chart-container {
    margin: 20px 0;
    padding: 10px;
    background-color: #fff;
    border-radius: 8px;
    box-shadow: 0px 2px 5px rgba(0, 0, 0, 0.1);
}

